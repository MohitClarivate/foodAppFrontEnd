import { StaffListPipe } from './staff-list.pipe';

describe('StaffListPipe', () => {
  it('create an instance', () => {
    const pipe = new StaffListPipe();
    expect(pipe).toBeTruthy();
  });
});
