import { ManagerListPipe } from './manager-list.pipe';

describe('ManagerListPipe', () => {
  it('create an instance', () => {
    const pipe = new ManagerListPipe();
    expect(pipe).toBeTruthy();
  });
});
